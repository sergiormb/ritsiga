{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    <strong>Imagen:</strong>
    </td>
    <td>
    <div>
        {% if object.image != null %}
            <img width="400" src="{{ asset('/images/' ~ object.image) }}">
        {% else %}
            <span>No picture</span>
        {% endif %}
    </div>
{% endblock %}