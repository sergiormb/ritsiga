{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') %}
    <a href="{{ path('sonata_admin_dashboard', {'code': object.domain}) }}" class="btn btn-sm btn-default view_link" title="{{ 'action_show'|trans({}, 'SonataAdminBundle') }}">
        <i class="glyphicon glyphicon-zoom-in"></i>
        {{ 'action_show'|trans({}, 'SonataAdminBundle') }}
    </a>
{% endif %}